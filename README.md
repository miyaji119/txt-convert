# TXT 优化器与 EPUB 转换器

模块化 Python 脚本，用于优化 TXT 小说文件并转换为 EPUB 格式。提供命令行（CLI）和图形界面（GUI）两种使用方式。

## 功能特性

### 🖥️ 双模式使用
- **命令行模式**：批量处理、自动化脚本
- **图形界面模式**：可视化操作、降低使用门槛

### TXT 文件优化
- 自动检测文件编码（支持 UTF-8、GBK、GB2312 等）
- 标准化章节标题格式
- 智能段落合并与分隔
- 支持多种章节格式识别

### EPUB 生成
- 自动解析章节标题
- 提取小说标题和作者信息
- 支持自定义封面图片
- 自动搜索网络封面（晋江、长佩、起点等平台）
- 保留文案/简介内容在第一章前

### 章节识别支持
| 格式 | 示例 |
|------|------|
| 标准格式 | `第1章 标题` / `第一章 标题` |
| 等号分隔 | `==第1章 标题==` |
| 数字序号 | `1、第一章 标题` |
| 特殊章节 | `楔子` / `番外` |

## 项目结构

```
novel-transfer/
├── txt_optimizer.py                  # CLI 主入口
├── gui.py                            # GUI 主入口 ⭐ NEW
├── encoding.py                       # 编码检测
├── chapter.py                        # 章节分析
├── chapter_config.py                 # 章节识别配置
├── display.py                        # 目录显示
├── cover.py                          # 封面下载
├── easypub.py                        # EasyPub优化
├── epub.py                           # EPUB生成
└── clean_duplicate_chapter_titles.py # 重复章节标题清理
```

## 安装依赖

```bash
# 基础依赖（GUI + TXT 转换）
# tkinter 已内置于 Python 标准库，无需安装

# 可选依赖（EPUB 生成 + 封面下载）
pip3 install ebooklib pillow requests
```

## 快速开始

### 🚀 启动图形界面（推荐新手）

```bash
python3 gui.py
```

启动后将显示如下界面：

```
┌──────────────────────────────────────────────────┐
│  TXT 转 EPUB 工具 v1.4                    [状态]  │
├──────────────────────────────────────────────────┤
│ [单文件转换] [批量转换] [EPUB生成] [章节目录]      │
├──────────────────────────────────────────────────┤
│           操作区域（根据标签页切换）               │
├──────────────────────────────────────────────────┤
│ 日志输出（彩色显示 INFO/WARN/ERROR）              │
├──────────────────────────────────────────────────┤
│ [████████░░] 60%   [开始] [取消] [打开输出目录]   │
└──────────────────────────────────────────────────┘
```

### 💻 使用命令行

```bash
python3 txt_optimizer.py --convert ~/Downloads/novel.txt
```

## GUI 使用说明

### 1. 单文件转换标签页

将单个 TXT 文件转换为 EPUB-ready 格式。

**操作流程**：
1. 点击「浏览...」选择 TXT 文件
2. 系统自动提取书名、作者（可手动修改）
3. 勾选「转换后显示章节目录」（可选）
4. 点击「▶ 开始转换」
5. 完成后点击「打开输出目录」查看结果

### 2. 批量转换标签页

批量处理文件夹内所有 TXT 文件。

**操作流程**：
1. 点击「浏览...」选择文件夹
2. 点击「▶ 开始批量转换」
3. 处理结果以表格形式展示（文件名、状态、章节数、大小）

### 3. EPUB 生成标签页

将 `*_epub_ready.txt` 文件转换为 EPUB 电子书。

**操作流程**：
1. 点击「浏览...」选择 TXT 文件
2. 自动加载并显示章节目录（章节数、行数、字数、章节列表）
3. 输入书名和作者（可手动修改）
4. 选择封面选项
5. 点击「▶ 生成 EPUB」
6. 如需重新加载章节目录，点击「↻ 重新加载章节」

**展示内容**：
- 顶部统计：章节数、总行数、总字数
- 章节列表表格：章节号、标题、行数、起始行、结束行、字数
- 封面选项区：4 种封面模式选择

**封面选项**：
- 不使用封面
- 本地图片路径（支持 jpg/png/gif/webp）
- 图片 URL（在线图片地址）
- 自动从网上搜索（推荐，支持晋江、长佩、起点等平台）

### 4. 章节目录标签页

查看并编辑 TXT 文件的章节列表和统计信息。

**展示内容**：
- 章节数、总行数、总字数（顶部统计）
- 章节列表表格：章节号、标题、行数、起始行、结束行、字数

**编辑功能**：
- 直接编辑章节号和标题（桌面 GUI 在编辑框中修改后点击「✓ 应用修改」，Web GUI 直接内联编辑）
- 上移/下移章节（自动重新编号）
- 删除章节（正文内容一并移除）
- 保存修改生成 `_edited.txt` 文件

### GUI 特性

- **后台线程执行**：所有耗时操作在后台线程运行，界面不冻结
- **实时日志输出**：彩色显示不同级别日志（INFO/WARN/ERROR/DEBUG）
- **进度条**：可视化任务执行进度
- **任务取消**：可随时取消运行中的任务
- **快速打开输出目录**：一键打开生成的文件所在目录
- **菜单快捷操作**：通过菜单栏快速打开文件/文件夹
- **章节目录编辑**：支持编辑章节号、标题，上移/下移/删除章节，保存修改到文件

## 使用方法

### 命令行模式

#### 1. 优化 TXT 文件（生成 *epub_ready.txt）
```bash
python3 txt_optimizer.py --convert ~/Downloads/novel.txt
```

#### 2. 生成 EPUB 文件
```bash
python3 txt_optimizer.py --epub ~/Downloads/novel_epub_ready.txt
```

#### 3. 指定书名和作者
```bash
python3 txt_optimizer.py --epub novel.txt --title "书名" --author "作者"
```

#### 4. 指定封面图片
```bash
python3 txt_optimizer.py --epub novel.txt --cover ~/Downloads/cover.jpg
```

#### 5. 自动搜索封面（推荐）
```bash
python3 txt_optimizer.py --epub novel.txt --auto-cover
```

#### 6. 查看章节目录
```bash
python3 txt_optimizer.py --catalog ~/Downloads/novel.txt
```

#### 7. 查看目录结构
```bash
python3 txt_optimizer.py --tree ~/Downloads/
```

#### 8. 批量转换文件夹
```bash
python3 txt_optimizer.py --batch ~/Downloads/novels/
```

#### 9. 显示帮助
```bash
python3 txt_optimizer.py --guide
```

## 命令行参数

| 参数 | 说明 |
|------|------|
| `--convert <文件>` | 转换单个文件为EasyPub友好格式 |
| `--batch <文件夹>` | 批量转换文件夹内的所有txt文件 |
| `--epub <文件>` | 将优化后的TXT文件转换为EPUB |
| `--tree <路径>` | 显示文件或目录结构 |
| `--catalog <文件>` | 显示TXT文件的章节目录 |
| `--guide` | 显示使用指南 |
| `--author <作者>` | 指定作者名 |
| `--title <书名>` | 指定书名 |
| `--cover <图片>` | 指定封面图片路径 |
| `--cover-url <URL>` | 指定封面图片URL |
| `--auto-cover` | 自动搜索封面 |

## 工作流程

```
原始TXT文件
    │
    ▼
┌─────────────────┐
│  --convert      │  标准化章节格式
│  生成*_epub_ready.txt │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│  --epub         │  生成EPUB
│  (可选封面搜索)  │
└─────────────────┘
    │
    ▼
   EPUB文件
```

## 支持的章节格式

脚本自动识别多种章节格式：

| 格式类型 | 示例 | 说明 |
|---------|------|------|
| 标准章节 | `第1章 标题`、`第一章 标题` | 支持中文数字和阿拉伯数字 |
| 特殊前缀 | `◇ 第20章 标题`、`◆ 第25章 标题` | 支持 ◇、◆、*、•、· 等符号前缀 |
| 方括号编号 | `[289]福利番外`、`[100]标题` | 方括号包裹的数字编号 |
| 分卷名 | `第一案：标题`、`第二卷：标题` | 以"案"或"卷"结尾的分卷名称 |
| 楔子 | `楔子`、`楔子 标题` | 小说序章 |
| 番外 | `番外1 标题`、`番外一 标题` | 额外故事章节 |
| 简单编号 | `1、第1章 标题` | 数字+顿号格式 |

## 输出文件

- 优化后的 TXT：`{书名}_epub_ready.txt`
- 章节分析：`{书名}_analysis.json`
- 生成的 EPUB：`{书名}.epub`
- 批量报告：`batch_report.md`

## 支持的平台

- macOS ✓
- Windows ✓
- Linux ✓

## 封面搜索来源

1. 晋江文学城 (jjwxc.net)
2. 长佩文学 (gongzicp.com)
3. 起点中文网 (qidian.com)
4. Open Library
5. Bing 图片搜索

## 修改记录

### v1.7 (2026-08-09)
- 修复 GUI 章节号无法编辑的问题（移除 `state='readonly'`）
- 修复 GUI 章节标题编辑后无法保存的问题（补充缺失的 `import re` 和 `from pathlib import Path`）
- 修复 GUI 章节保存功能不生效的问题：保存前自动应用编辑框中未提交的修改
- 优化保存逻辑：改为同步执行，添加完整的错误处理和错误提示
- 优化 GUI 界面风格：添加标题分隔线、Treeview 表格样式、修正修改行高亮颜色
- 章节编辑支持同时修改章节号和标题

### v1.6 (2026-05-17)
- 优化 EPUB 生成标签页，新增章节目录显示功能
- 选择 TXT 文件后自动加载章节列表（章节数、行数、字数）
- 新增「↻ 重新加载章节」按钮，支持手动刷新章节目录
- 修复 macOS Tk 文件对话框 filetypes 兼容性问题（移除 `*.*`、`*_epub_ready.txt`）

### v1.5 (2026-05-17)
- 🎉 新增图形化界面（GUI），基于 tkinter + ttk 实现
- GUI 支持单文件转换、批量转换、EPUB 生成、章节目录查看四大功能模块
- GUI 采用后台线程执行任务，避免界面冻结
- GUI 提供彩色日志输出、进度条、任务取消等现代化交互
- 修复后台线程在主窗口关闭时的异常处理

### v1.4 (2026-05-17)
- 修复章节识别误判问题（如文案描述行 `1、1v1，xxx` 被误识别为章节）
- 新增文案描述行过滤规则（支持关键词：`1v1`、`he`、`be`、`np`、`攻`、`受`、`慎入`、`避雷`等）
- 优化章节验证逻辑，同时检查章节号和标题匹配
- 修复 `easypub.py` 中的逻辑错误，避免验证结果被覆盖
- 删除重复的 `clean_chapter_titles.py` 文件，保留功能更完善的 `clean_duplicate_chapter_titles.py`
- 统一 `easypub.py` 和 `chapter.py` 的章节识别逻辑，确保一致性

### v1.3 (2026-05-16)
- 新增分卷名识别支持（如 `第一案：标题`、`第二卷：标题`）
- 新增特殊字符前缀章节支持（如 `◇ 第20章`、`◆ 第25章`）
- 新增方括号编号章节支持（如 `[289]福利番外`）
- 新增独立数字行章节支持（如单独一行 `47` 后跟 `第47章`）
- 修复章节标题中数字前后有空格的识别问题（如 `===第 220 章 番外===`）
- 新增数字+标题连在一起的章节格式支持（如 `29同心结`）
- 修复UTF-8 BOM字符导致书名提取失败的问题
- 优化章节识别逻辑，避免章节内小标题被误识别为独立章节
- 修复书名提取逻辑，只在文件开头部分搜索，避免误匹配内容中的书名
- 修复书名前缀识别问题，正确移除"书名："、"题名："等前缀
- 修复书名中方括号 `[]` 被移除的问题，保留书名原始格式
- 新增书名提取和封面下载的详细调试日志，方便排查问题
- 优化段落合并逻辑，正确处理连续空行，避免产生过多段落
- 新增章节内容清理功能，移除章节末尾的单独括号等冗余字符

### v1.2 (2026-05-15)
- 新增自动提取书名和作者功能
- 支持多种书名格式：`《书名》作者`、`书名\n作者：`
- 新增 `--cover-url` 参数支持指定封面图片URL

### v1.1 (2026-05-14)
- 脚本重构为模块化结构
- 支持多种章节格式识别
- 新增番外章节处理逻辑
